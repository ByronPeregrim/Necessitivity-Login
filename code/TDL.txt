Login:

Reset Password

User:

Calories burned display boxes execute one retrieve per day, need to change this to execute a single retrieve for all days.

Admin:

Clear buttons, clears entries and removes user results modal